import{_ as e,o as i,c as a,al as t}from"./chunks/framework.7vp1BMOE.js";const k=JSON.parse('{"title":"Building SITL Containers","description":"","frontmatter":{},"headers":[],"relativePath":"en/dev_setup/sitl_container_builds.md","filePath":"en/dev_setup/sitl_container_builds.md"}'),n={name:"en/dev_setup/sitl_container_builds.md"};function l(o,s,h,r,p,d){return i(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="building-sitl-containers" tabindex="-1">Building SITL Containers <a class="header-anchor" href="#building-sitl-containers" aria-label="Permalink to &quot;Building SITL Containers&quot;">​</a></h1><p>The <a href="./../simulation/px4_sitl_prebuilt_packages">prebuilt SITL images</a> share the existing <code>.deb</code> build and publishing workflow. <code>Tools/packaging/containers/docker-bake.hcl</code> defines the image graph, tags and cache settings; <code>.github/workflows/build_deb_package.yml</code> supplies the simulator, architecture and version.</p><h2 id="source-layout" tabindex="-1">Source Layout <a class="header-anchor" href="#source-layout" aria-label="Permalink to &quot;Source Layout&quot;">​</a></h2><p>Files are grouped by responsibility:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Tools/packaging/</span></span>
<span class="line"><span>  deb/</span></span>
<span class="line"><span>    gazebo/             Package lifecycle hooks and the installed Gazebo launcher</span></span>
<span class="line"><span>    sih/                Package lifecycle hooks</span></span>
<span class="line"><span>  containers/</span></span>
<span class="line"><span>    Dockerfile.*        SIH, Gazebo and ROS image definitions</span></span>
<span class="line"><span>    docker-bake.hcl     Connected image build and publishing targets</span></span>
<span class="line"><span>    prepare_context.sh Build-context staging</span></span>
<span class="line"><span>    *-entrypoint.sh     Container startup scripts</span></span>
<span class="line"><span>    px4-network.sh     Shared runtime host routing</span></span>
<span class="line"><span>    ros2-install-dependencies.sh</span></span>
<span class="line"><span>    ros2-agent.repos</span></span>
<span class="line"><span>    test_sih_mission.py</span></span>
<span class="line"><span>Tools/ros2/</span></span>
<span class="line"><span>  prepare_workspace.py Checkout-matched ROS source workspace preparation</span></span>
<span class="line"><span>  ros2.repos           Shared immutable source pins for images and integration CI</span></span></code></pre></div><p>The generated <code>docker-context/</code> remains a flat staging directory; it is not the source layout. ROS workspace preparation is separate from packaging because it is also used for development and integration tests.</p><h2 id="build-and-publish-flow" tabindex="-1">Build And Publish Flow <a class="header-anchor" href="#build-and-publish-flow" aria-label="Permalink to &quot;Build And Publish Flow&quot;">​</a></h2><ol><li>The package jobs build SIH and Gazebo <code>.deb</code>s for Ubuntu Noble and Jammy.</li><li>Four native container jobs consume the Noble packages: SIH/Gazebo on amd64/arm64. Each job runs <code>Tools/packaging/containers/prepare_context.sh</code> to stage the package context and matching PX4 messages, then builds the runtime image and its ROS child together with Docker Bake.</li><li>On release tags or a manual run with deployment enabled, the jobs push architecture-specific images to Docker Hub and GHCR. The deploy jobs combine those images into versioned and <code>latest</code> multi-architecture tags using <code>docker buildx imagetools create</code>.</li></ol><p>Pull requests build the same graph without publishing. The ROS image consumes its parent directly through a Bake target context, so neither local builds nor pull requests need an unpublished parent tag in a registry.</p><h2 id="building-locally" tabindex="-1">Building Locally <a class="header-anchor" href="#building-locally" aria-label="Permalink to &quot;Building Locally&quot;">​</a></h2><p>Run these commands from the repository root on Ubuntu 24.04 with <a href="./dev_env_linux_ubuntu">PX4 build dependencies</a> and Docker Buildx installed:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4_sitl_sih</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/px4_sitl_sih</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cpack</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -G</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> DEB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-context</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/px4_sitl_sih/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">*</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.deb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-context/</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Tools/packaging/containers/prepare_context.sh</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SIMULATOR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">sih</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> buildx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Tools/packaging/containers/docker-bake.hcl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --load</span></span></code></pre></div><p>On macOS or Windows, obtain Noble <code>.deb</code>s for the same PX4 revision and Docker architecture instead of building the packages natively, then start at the context-preparation step. Do not combine packages from different revisions or architectures in one context. For Gazebo, build <code>px4_sitl_default</code> and set <code>SIMULATOR=gazebo</code>.</p><p><code>ARCH</code> defaults to the Docker builder&#39;s local architecture and <code>VERSION</code> defaults to <code>dev</code>. For example, an ARM64 SIH build produces <code>px4io/px4-sitl-ros2:dev-arm64</code>. Override <code>VERSION</code> and <code>ARCH</code> through environment variables when needed. Replace <code>--load</code> with <code>--print</code> to inspect the resolved graph without building, or add <code>--set ros2.tags=px4-ros2:local</code> to choose a local ROS image tag.</p><h2 id="cache-boundaries" tabindex="-1">Cache Boundaries <a class="header-anchor" href="#cache-boundaries" aria-label="Permalink to &quot;Cache Boundaries&quot;">​</a></h2><p>Every successful <code>RUN</code> creates a cacheable layer. A failed step is not committed as an image layer. Changing an input invalidates its layer and subsequent layers, not earlier ones.</p><p><code>Tools/ros2/ros2.repos</code> pins the ROS repositories to immutable commits for both images and integration tests. The interface-library pin includes the manual WaitSet ownership fixes merged in <a href="https://github.com/Auterion/px4-ros2-interface-lib/pull/222" target="_blank" rel="noreferrer">Auterion/px4-ros2-interface-lib#222</a>. To test another interface-library commit in an image, pass <code>--set ros2.args.PX4_ROS2_REF=&lt;40-hex-SHA&gt;</code> to Bake. <code>PX4_MSGS_REF</code> can similarly override the message-package metadata; the definitions always come from the PX4 checkout. The base ROS/rosdep setup, DDS Agent, message-package metadata, interface-library sources, workspace dependency installation, compilation and tests have separate cache boundaries. Message definitions are copied after dependency installation, so changing them does not repeat <code>rosdep install</code>. Repository checkouts live in a separate source stage and are mounted read-only for dependency installation. The development toolchain does not inherit those source layers; only the ROS workspace builder copies the checkouts. Changing only the ROS entrypoint does not rebuild the workspace, and changing only the test command does not recompile it.</p><p>For the packaged ROS images, the Agent and ROS workspace build in independent Ubuntu 24.04/Jazzy stages, keyed to source pins and the supplied PX4 message definitions rather than the packaged firmware. A new PX4 binary with unchanged messages reuses those compilation and test layers. Changed messages invalidate workspace compilation and tests; cached artifacts never substitute messages from another checkout. The final image still derives from the connected SIH/Gazebo parent and installs its Ubuntu/ROS dependencies using the same helper as the builder. Only the source-built Agent/logger and complete ROS workspace are copied, preserving the source/build/install layout and absolute symlinks without copying the builder&#39;s operating system or package database. The Agent installation is staged with CMake&#39;s <code>DESTDIR</code>, so copying it cannot overwrite Python packages installed separately in the destination image. APT cache mounts are held only during installation, not during compilation or tests. The runtime Dockerfiles extract the package&#39;s <code>Depends</code> field into a separate stage, so their dependency layers are keyed to that field rather than to every firmware binary change. Package jobs disable Ubuntu&#39;s container cleanup hook so downloaded APT archives survive installation and can be cached between runs.</p><p>In Actions, <code>CACHE_GHA=true</code> enables one cache per simulator and architecture. The ROS target exports <code>mode=max</code>, covering its entire graph including the runtime parent. The compiler cache is persisted separately with <code>actions/cache</code> and <code>buildkit-cache-dance</code>, allowing unchanged compilation units to be reused when workspace inputs change. Ccache checks compiler contents and source/header inputs; it does not substitute an old ROS workspace for changed PX4 messages. The container jobs use eight native CPUs, matching the Dockerfile&#39;s <code>BUILD_JOBS</code> default. Override that build argument with Bake&#39;s <code>--set ros2.args.BUILD_JOBS=4</code> when building on a smaller machine. Local builds use the builder&#39;s regular cache by default; standard Bake <code>--set</code> options can select other cache backends. Cached installs do not automatically refresh upstream packages: rebuild without cache when intentionally refreshing dependencies.</p><h2 id="updating-ros-source-pins" tabindex="-1">Updating ROS Source Pins <a class="header-anchor" href="#updating-ros-source-pins" aria-label="Permalink to &quot;Updating ROS Source Pins&quot;">​</a></h2><p>Source pins are maintained manually through reviewed pull requests, not advanced automatically when images are built. Update the full commit SHA in <code>Tools/ros2/ros2.repos</code> when adopting upstream changes. The same manifest is consumed by both container builds and ROS integration CI; run both workflows before accepting an update.</p><p>The <code>px4_msgs</code> pin selects package metadata and build logic, not the message definitions used to build the workspace. Definitions always come from the PX4 checkout, so ordinary message changes do not require a pin update. Image and workflow SHA overrides are for testing candidates; adopting a candidate requires updating the shared manifest.</p><h2 id="testing-a-px4-checkout-with-ros" tabindex="-1">Testing A PX4 Checkout With ROS <a class="header-anchor" href="#testing-a-px4-checkout-with-ros" aria-label="Permalink to &quot;Testing A PX4 Checkout With ROS&quot;">​</a></h2><p>The standalone <code>ghcr.io/px4/px4-dev-ros2</code> image provides Ubuntu 22.04/ROS Humble (<code>main-humble</code>) and Ubuntu 24.04/ROS Jazzy (<code>main-jazzy</code>) toolchains for native <code>amd64</code> and <code>arm64</code>. Both include Micro XRCE-DDS Agent v2.4.3, PX4&#39;s source-build dependencies and the dependencies required by the pinned ROS repositories. They are also available on Docker Hub as <code>px4io/px4-dev-ros2</code>, with matching tags in both registries. They contain no repository checkouts, packaged PX4 firmware or compiled ROS workspace, and their entrypoint sources only the selected ROS underlay. The packaged SIH/Gazebo ROS images remain Ubuntu 24.04/Jazzy only. Both Agent builds statically link the same private Fast DDS 2.14.7/Fast CDR 2.2.8 dependencies pinned in <code>Tools/packaging/containers/ros2-agent.repos</code>. This provides one controlled Agent dependency set across both distros and avoids Humble&#39;s Fast CDR 1.x build incompatibility with Agent v2.4.3. Their resolved revisions and licences are retained under <code>/usr/local/share/micro-xrce-dds-agent</code>; neither distro&#39;s ROS libraries are replaced. The packaged Jazzy ROS images reuse this Agent build. These toolchains are published independently of the SITL package workflow, so they do not wait for <code>.deb</code> builds. Publishing follows the same release-tag and opt-in manual policy as <code>px4-dev</code>.</p><p>The <code>main-humble</code> and <code>main-jazzy</code> tags name manually published toolchains for PX4 <code>main</code> development, not bundled firmware. Pushing a <code>v*</code> Git tag publishes both <code>&lt;git-tag&gt;-humble</code> and <code>&lt;git-tag&gt;-jazzy</code>, such as <code>v1.18.0-humble</code> and <code>v1.18.0-jazzy</code>, retaining the leading <code>v</code> without updating either <code>main-*</code> tag. Each publication also provides <code>sha-&lt;full-PX4-commit&gt;-&lt;ros-distro&gt;</code>, identifying the repository revision used to build the toolchain. Release-line toolchains can use tags such as <code>v1.18-humble</code> and <code>v1.18-jazzy</code> through the manual publisher&#39;s <code>tag</code> prefix input (<code>v1.18</code>) when that release line is maintained. The source revision and intended PX4 release line do not change which firmware a checkout builds. ROS integration CI pins the image by digest so publishing a new moving tag cannot silently change the test environment. When adopting a rebuilt toolchain, update the digest in <code>.github/workflows/ros_integration_tests.yml</code>.</p><p>The optional <code>ros2-dev</code> Bake target builds this image without <code>.deb</code> packages or a published SITL parent:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Tools/packaging/containers/prepare_context.sh</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ROS_DISTRO</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">humble</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> buildx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Tools/packaging/containers/docker-bake.hcl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ros2-dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ros2-dev.tags=px4-dev-ros2:local</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --load</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SOURCE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pwd</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -P</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GIT_COMMON_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rev-parse</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --path-format=absolute</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --git-common-dir</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -it</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$SOURCE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$SOURCE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$GIT_COMMON_DIR</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$GIT_COMMON_DIR</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -w</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$SOURCE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> GIT_CONFIG_COUNT=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> GIT_CONFIG_KEY_0=safe.directory</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;GIT_CONFIG_VALUE_0=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$SOURCE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4-dev-ros2:local</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span></span></code></pre></div><p>The same absolute mounts support both ordinary checkouts and git worktrees. Set <code>ROS_DISTRO=jazzy</code> instead, or omit it, for the default Jazzy developer image. Bake selects the matching Ubuntu base; only <code>humble</code> and <code>jazzy</code> are supported. This selection applies only to <code>ros2-dev</code>, not the packaged runtime targets. The Git ownership exception is scoped to this container and checkout. Inside the container, build the current checkout; the build initialises its required submodules:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PX4_DIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$PWD</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4_sitl_sih</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;/opt/ros/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$ROS_DISTRO</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/setup.bash&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Tools/ros2/prepare_workspace.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/px4_ros2_ci</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/px4_ros2_ci</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt-get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -qq</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rosdep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rosdistro</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$ROS_DISTRO</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rosdep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --from-paths</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> src</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ignore-src</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --rosdistro</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$ROS_DISTRO</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -y</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CMAKE_BUILD_PARALLEL_LEVEL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">8</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> colcon</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --symlink-install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --executor</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sequential</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --cmake-args</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -DCMAKE_BUILD_TYPE=Release</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DBUILD_TESTING=ON</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -DCMAKE_C_COMPILER_LAUNCHER=ccache</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -DCMAKE_CXX_COMPILER_LAUNCHER=ccache</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">colcon</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --packages-select</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4_ros2_cpp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ctest-args</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -R</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> unit_tests</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">colcon</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test-result</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --verbose</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install/setup.bash</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$PX4_DIR</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/ros_test_runner.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --config-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/ros_tests/config-sih.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --build-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build/px4_sitl_sih</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --model</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> quadx</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --px4-ros2-interface-lib-build-dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /opt/px4_ros2_ci/build/px4_ros2_cpp</span></span></code></pre></div><p>The workspace helper requires a new or empty directory, imports the standard <code>ros2.repos</code> manifest with vcstool, and replaces message/service definitions with those from the current PX4 checkout, including removal of deleted definitions. Each run imports the pinned repositories directly with vcstool, without reusing an existing source or compiled workspace. It preserves all library packages, examples and Python tests. An optional second argument selects another interface-library commit using its full 40-character SHA. The helper prints the resolved source manifest, repository commits and PX4 commit; it does not build the workspace.</p><p>ROS integration CI builds PX4 afresh for each pull request and only sources the Jazzy underlay before building this separate workspace. Its manual <code>px4_ros2_ref</code> input selects a full interface-library commit SHA; leaving it empty uses <code>ros2.repos</code>. CI prepares and builds the ROS workspace on every run using the current checkout&#39;s message/service definitions. Only the compiler cache is restored, not an old source, build or install workspace. Ccache validates compiler contents and compilation inputs, including changed library sources and generated messages. <code>CACHE_GHA=true</code> uses an independent <code>ros2-dev-&lt;ros-distro&gt;-&lt;architecture&gt;</code> image cache, without replacing the published SITL image caches. The <code>ros_dev_container.yml</code> workflow builds both standalone variants on every <code>main</code> push and on pull requests that change its watched paths, without publishing. In the upstream repository, it publishes to Docker Hub and GHCR only on <code>v*</code> tag pushes or authorised manual runs with <code>deploy_to_registry=true</code>. Manual runs build both distros and now take a distro-independent <code>tag</code> prefix, defaulting to <code>main</code>, rather than a complete tag such as <code>main-jazzy</code>. Use <code>main</code> to publish <code>main-humble</code> and <code>main-jazzy</code>, or <code>v1.18</code> to publish <code>v1.18-humble</code> and <code>v1.18-jazzy</code>; ROS-suffixed inputs are rejected to avoid mislabelling images. The existing <code>build_ref</code> selects the source revision, and the deployment toggle also applies when dispatching against a Git tag. Architecture tags use <code>sha-&lt;full-PX4-commit&gt;-&lt;ros-distro&gt;-&lt;architecture&gt;</code>; each distro&#39;s selected tag and <code>sha-&lt;full-PX4-commit&gt;-&lt;ros-distro&gt;</code> indexes preserve its two architectures and their SBOMs. Use <code>--set ros2-dev.args.PX4_ROS2_REF=&lt;40-hex-SHA&gt;</code> to build the development image with another interface-library commit.</p><p>Both ROS development images and the CI toolchain reuse <code>Tools/setup/ubuntu.sh --no-nuttx --no-sim-tools</code> and its Python requirements. Already installed Ubuntu/ROS Python versions are constrained during installation instead of gratuitously upgrading the ROS stack. The SIH image does not acquire Gazebo or the NuttX toolchain. Installing the complete source-build toolset increases cold image-build time and size; existing native architecture and incremental cache boundaries remain in use.</p><h2 id="container-sboms" tabindex="-1">Container SBOMs <a class="header-anchor" href="#container-sboms" aria-label="Permalink to &quot;Container SBOMs&quot;">​</a></h2><p>The publishing workflow enables BuildKit&#39;s standard SBOM attestations for both runtime and ROS images. It pins BuildKit v0.33.0, whose 80 MiB attestation limit accommodates the Gazebo/ROS SPDX documents without reducing their package or file coverage. Local attested builds using BuildKit v0.32 can fail at export because that version limits each attestation to 40 MiB. These SPDX inventories describe discoverable packages in the image filesystem, including Ubuntu and ROS dependencies. They complement, rather than replace, PX4&#39;s <a href="./../contribute/sbom">source and firmware SBOM</a>.</p><p>Each architecture is published with its attestation. The final <code>imagetools create</code> step preserves those attestations when assembling multi-architecture indexes. Inspect a published image with:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> buildx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> imagetools</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> inspect</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> px4io/px4-sitl-ros2:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --format</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{{json .SBOM}}&#39;</span></span></code></pre></div><p>Non-publishing pull-request builds do not publish an SBOM for inspection. Local <code>--load</code> builds are intended for running examples; use a registry or an OCI export when retaining attestations.</p>`,37)])])}const g=e(n,[["render",l]]);export{k as __pageData,g as default};
