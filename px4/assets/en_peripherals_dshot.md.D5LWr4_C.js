import{_ as a,C as r,o as i,c as n,al as t,E as s}from"./chunks/framework.7vp1BMOE.js";const g=JSON.parse('{"title":"DShot ESCs","description":"","frontmatter":{},"headers":[],"relativePath":"en/peripherals/dshot.md","filePath":"en/peripherals/dshot.md"}'),l={name:"en/peripherals/dshot.md"};function h(c,e,d,p,u,m){const o=r("Badge");return i(),n("div",null,[e[0]||(e[0]=t('<h1 id="dshot-escs" tabindex="-1">DShot ESCs <a class="header-anchor" href="#dshot-escs" aria-label="Permalink to &quot;DShot ESCs&quot;">​</a></h1><p>DShot is an alternative ESC protocol that has several advantages over <a href="./../peripherals/pwm_escs_and_servo">PWM</a> or <a href="./../peripherals/oneshot">OneShot</a>:</p><ul><li>Reduced latency.</li><li>Increased robustness via a checksum.</li><li>No need for ESC calibration as the protocol uses digital encoding.</li><li>Telemetry feedback is available/supported on some ESCs.</li><li>Can reverse motor spin directions via commands when needed (rather than physically moving wires/re-soldering).</li><li>Other useful commands are supported.</li></ul><p>This topic shows how to connect and configure DShot ESCs.</p><h2 id="supported-esc" tabindex="-1">Supported ESC <a class="header-anchor" href="#supported-esc" aria-label="Permalink to &quot;Supported ESC&quot;">​</a></h2><p><a href="./../peripherals/esc_motors#supported-esc">ESCs &amp; Motors &gt; Supported ESCs</a> has a list of supported ESC (check &quot;Protocols&quot; column for DShot ESC).</p><h2 id="wiring" tabindex="-1">Wiring/Connections <a class="header-anchor" href="#wiring" aria-label="Permalink to &quot;Wiring/Connections {#wiring}&quot;">​</a></h2><p>DShot ESC are wired the same way as <a href="./pwm_escs_and_servo">PWM ESCs</a>. The only difference is that they can only be connected to the FMU, and usually only to some subset of pins.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>You may want to check the actuator configuration screen to see what pins are available for DShot on your controller before wiring up!</p></div><p>Pixhawk controllers with both an FMU and an IO board usually label them as <code>AUX</code> (FMU) and <code>MAIN</code> (IO), respectively. These match the <code>PWM AUX</code> and <code>PWM MAIN</code> output tabs on the actuator configuration screen. For these controllers connect the DShot ESC to the <code>AUX</code> port.</p><p>Controllers that don&#39;t have an IO board usually label the (single) output port as <code>MAIN</code>, and this is where you will connect your DShot ESC. If the controller without IO has its own firmware, the actuator assignment will be to the matching <code>PWM MAIN</code> outputs. However if the same firmware is used for hardware with/without the IO board, such as for the Pixhawk 4 and Pixhawk 4 Mini, then actuator assignment tab used is the same in both cases: <code>PWM AUX</code> (i.e. not matching the port label <code>MAIN</code> in the &quot;mini&quot; case).</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Remove propellers before changing ESC configuration parameters!</p></div><p>Enable DShot for your required outputs in the <a href="./../config/actuators">Actuator Configuration</a>.</p><p>DShot comes with different speed options: <em>DShot150</em>, <em>DShot300</em>, and <em>DShot600</em> where the number indicates the speed in kilo-bits/second. You should set the parameter to the highest speed supported by your ESC (according to its datasheet).</p><p>Then connect the battery and arm the vehicle. The ESCs should initialize and the motors turn in the correct directions.</p><ul><li>If the motors do not spin in the correct direction (for the <a href="./../airframes/airframe_reference">selected airframe</a>) you can reverse them in the UI using the <strong>Set Spin Direction</strong> option (this option appears after you select DShot and assign motors).</li></ul><h2 id="commands" tabindex="-1">ESC Commands <a class="header-anchor" href="#commands" aria-label="Permalink to &quot;ESC Commands {#commands}&quot;">​</a></h2><p>Commands can be sent to the ESC via the <a href="./../debug/mavlink_shell">MAVLink shell</a>. See the <a href="./../modules/modules_driver#dshot"><code>dshot</code> module</a> for a full reference of the supported commands.</p><h2 id="esc-telemetry" tabindex="-1">ESC Telemetry <a class="header-anchor" href="#esc-telemetry" aria-label="Permalink to &quot;ESC Telemetry&quot;">​</a></h2><p>Some ESCs are capable of sending telemetry back to the flight controller through a UART RX port. These DShot ESCs will have an additional telemetry wire.</p><p>The provided telemetry includes:</p><ul><li>Temperature</li><li>Voltage</li><li>Current</li><li>Accumulated current consumption</li><li>RPM values</li></ul><p>To enable this feature (on ESCs that support it):</p><ol><li>Join all the telemetry wires from all the ESCs together, and then connect them to one of the RX pins on an unused flight controller serial port.</li><li>Enable telemetry on that serial port using <a href="./../advanced_config/parameter_reference#DSHOT_TEL_CFG">DSHOT_TEL_CFG</a>.</li></ol><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>You may have to configure the per-motor pole count parameters (<a href="./../advanced_config/parameter_reference#DSHOT_MOT_POL1"><code>DSHOT_MOT_POL1</code>–<code>DSHOT_MOT_POL12</code></a>) to get correct RPM values. The default value for these is 14 poles, which is typical for 5-inch prop motors.</p></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p><a href="#extended-dshot-telemetry-edt">Extended DShot Telemetry (EDT)</a> can provide temperature, voltage, and current through the BDShot signal — no serial telemetry wire needed.</p></div><h2 id="bidirectional-dshot-telemetry" tabindex="-1">Bidirectional DShot (Telemetry) <a class="header-anchor" href="#bidirectional-dshot-telemetry" aria-label="Permalink to &quot;Bidirectional DShot (Telemetry)&quot;">​</a></h2>',28)),s(o,{type:"tip",text:"PX4 v1.16"}),e[1]||(e[1]=t('<p>Bidirectional DShot (BDShot) enables the ESC to send eRPM telemetry back to the flight controller on the same signal wire used for throttle commands — no additional telemetry wire is needed for RPM data. High-rate eRPM data significantly improves the performance of <a href="./../config_mc/filter_tuning#dynamic-notch-filters">Dynamic Notch Filters</a> and enables more precise vehicle tuning.</p><p>With <a href="#extended-dshot-telemetry-edt">Extended DShot Telemetry (EDT)</a> enabled, BDShot can also provide temperature, voltage, and current data.</p><h3 id="hardware-support" tabindex="-1">Hardware Support <a class="header-anchor" href="#hardware-support" aria-label="Permalink to &quot;Hardware Support&quot;">​</a></h3><p>BDShot requires a flight controller with DMA-capable timers. Any FMU output on a supported timer can be used for BDShot — multiple timers are supported through sequential burst/capture.</p><p>Supported processors:</p><ul><li><strong>STM32H7</strong>: All FMU outputs on DMA-capable timers</li><li><strong>i.MXRT</strong> (V6X-RT &amp; Tropic): All FMU outputs</li></ul><div class="info custom-block"><p class="custom-block-title">INFO</p><p>The ESC must be connected to FMU outputs only. These are labeled <code>MAIN</code> on controllers with a single PWM bus, and <code>AUX</code> on controllers with both <code>MAIN</code> and <code>AUX</code> ports (i.e. those with an IO board).</p></div><h3 id="bidirectional-dshot-configuration" tabindex="-1">PX4 Configuration <a class="header-anchor" href="#bidirectional-dshot-configuration" aria-label="Permalink to &quot;PX4 Configuration {#bidirectional-dshot-configuration}&quot;">​</a></h3><p>BDShot is enabled <strong>per-timer</strong> in the <a href="./../config/actuators">Actuator Configuration</a> UI. Select <strong>BDShot150</strong>, <strong>BDShot300</strong>, or <strong>BDShot600</strong> as the output protocol instead of the corresponding DShot speed. There is no separate enable parameter — choosing a BDShot protocol activates bidirectional telemetry on that timer&#39;s outputs.</p><p>The system calculates actual motor RPM from eRPM data using per-motor pole count parameters: <code>DSHOT_MOT_POL1</code> through <code>DSHOT_MOT_POL12</code> (one per motor output). The default is 14 poles, which is typical for 5-inch prop motors. If you are using AM32 ESCs, the motor pole count must also be set in the AM32 firmware configuration (e.g. via the AM32 configurator tool) to match.</p><h3 id="extended-dshot-telemetry-edt" tabindex="-1">Extended DShot Telemetry (EDT) <a class="header-anchor" href="#extended-dshot-telemetry-edt" aria-label="Permalink to &quot;Extended DShot Telemetry (EDT)&quot;">​</a></h3><p>EDT extends BDShot by interleaving temperature, voltage, and current data into the eRPM telemetry frames. This allows ESC health monitoring through the same signal wire, without requiring a separate serial telemetry connection.</p><p>To enable EDT:</p><ol><li>Configure BDShot on the desired outputs (see above).</li><li>Set <code>DSHOT_BIDIR_EDT</code> to <code>1</code> and reboot.</li></ol><p>The ESC firmware must support EDT (e.g. <a href="https://github.com/am32-firmware/AM32" target="_blank" rel="noreferrer">AM32</a>).</p><p>When both serial telemetry and BDShot/EDT are enabled, the driver merges data from both sources.</p><h2 id="am32-esc-settings-eeprom" tabindex="-1">AM32 ESC Settings (EEPROM) <a class="header-anchor" href="#am32-esc-settings-eeprom" aria-label="Permalink to &quot;AM32 ESC Settings (EEPROM)&quot;">​</a></h2><p>PX4 can read and write AM32 ESC firmware settings (EEPROM) via a ground station, enabling remote ESC configuration without connecting directly to each ESC.</p><h3 id="requirements" tabindex="-1">Requirements <a class="header-anchor" href="#requirements" aria-label="Permalink to &quot;Requirements&quot;">​</a></h3><ul><li>ESCs running <a href="https://github.com/am32-firmware/AM32" target="_blank" rel="noreferrer">AM32 firmware</a> with serial telemetry connected (<a href="./../advanced_config/parameter_reference#DSHOT_TEL_CFG">DSHOT_TEL_CFG</a>)</li><li><code>DSHOT_ESC_TYPE</code> set to <code>1</code> (AM32)</li><li>Ground station with ESC EEPROM support (QGroundControl feature in development)</li><li>MAVLink development dialect enabled on the flight controller</li></ul><h3 id="how-it-works" tabindex="-1">How It Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How It Works&quot;">​</a></h3><p>PX4 automatically reads the full EEPROM from each ESC on boot. The ground station can then display individual settings and allow the user to modify them. Changes are written back to the ESC one byte at a time using the DShot programming protocol.</p>',22))])}const S=a(l,[["render",h]]);export{g as __pageData,S as default};
