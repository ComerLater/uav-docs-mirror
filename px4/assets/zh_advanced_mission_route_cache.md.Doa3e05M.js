import{_ as a,o as s,c as n,al as i}from"./chunks/framework.7vp1BMOE.js";const u=JSON.parse('{"title":"Mission Route Cache","description":"","frontmatter":{},"headers":[],"relativePath":"zh/advanced/mission_route_cache.md","filePath":"zh/advanced/mission_route_cache.md"}'),t={name:"zh/advanced/mission_route_cache.md"};function o(l,e,p,c,r,d){return s(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="mission-route-cache" tabindex="-1">Mission Route Cache <a class="header-anchor" href="#mission-route-cache" aria-label="Permalink to &quot;Mission Route Cache&quot;">​</a></h1><p>Normal mission execution keeps a small sliding window of mission items in RAM. Route-aware features may instead need random, non-blocking access to the complete route.</p><p>The mission route cache provides this data without reading Dataman or the SD card during planning:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>MissionRouteCache</span></span>
<span class="line"><span>|-- full mission route (optional) [0, ..., CONFIG_NAVIGATOR_FULL_MISSION_CACHE_SIZE - 1]</span></span>
<span class="line"><span>|-- safe points                  [all uploaded safe points]</span></span>
<span class="line"><span>\`-- published mission-land item  [one item]</span></span></code></pre></div><p>The cache is infrastructure for Navigator features. It does not change normal mission execution by itself.</p><h2 id="访问链接" tabindex="-1">访问链接 <a class="header-anchor" href="#访问链接" aria-label="Permalink to &quot;访问链接&quot;">​</a></h2><p>The full route is loaded into RAM asynchronously. It becomes available only after every mission item has loaded successfully. A partial route is never exposed.</p><p>When the cached mission identity changes, the old route becomes unavailable immediately and the replacement is loaded as a new generation. Failed reads keep the successfully loaded prefix and retry with a capped backoff.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If the full cache is disabled or the mission exceeds its capacity, a consuming feature must use its normal fallback behavior. The independent safe-point and mission-land caches remain available.</p></div><h2 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to &quot;配置&quot;">​</a></h2><p><code>CONFIG_NAVIGATOR_FULL_MISSION_CACHE_SIZE</code> sets the maximum number of items in the full route cache. It defaults to <code>500</code> on POSIX platforms such as SITL, and <code>0</code> on embedded targets unless a board overrides it.</p><p>A value of <code>0</code> removes the full-mission buffer and its Dataman client from the build. Boards that enable full-route planning must choose a capacity that fits their RAM budget.</p><p>The buffer is allocated once when Navigator starts and uses one <code>mission_item_s</code> entry per configured item. An entry is currently 56 bytes, so 500 items use 28 kB.</p><details class="details custom-block"><summary>Details</summary><p>Developer details: Loading workflow</p><p>The full mission is exposed as one complete generation:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>New mission accepted</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Read one item asynchronously</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Dataman response wakes Navigator</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Validate the response and queue the next item</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Repeat until every item is loaded</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Mark the complete generation ready</span></span></code></pre></div><p>Only one full-mission read is outstanding at a time. While it is pending, Navigator polls that client&#39;s response subscription, so each completion can queue the next read immediately. There is no separate cache thread.</p><p>The response topic is shared by all Dataman clients, so poll readiness alone does not mean this request completed. The response is still copied and matched to the request, and the subscription is polled only while this cache has a pending read.</p><p>Cache-only wakeups advance the load without running all Navigator state machines at the Dataman response rate. During a burst of Dataman-only wakeups, normal Navigator work stays on its existing cadence; a cache wake also runs it once the update period is due.</p><p>The cache identifies a mission source by its mission ID, item count, and Dataman bank. If the source changes while a read is in flight, that response is consumed but not accepted into the new generation.</p></details><details class="details custom-block"><summary>Details</summary><p>Developer details: Borrowing a mission view</p><p><code>getMissionView()</code> returns a zero-copy view of the cache-owned array. The view contains a pointer, item count, mission identity, and generation number; it does not own or copy the items.</p><p>A consumer should acquire a complete view, compute synchronously, and validate the same view before accepting the result:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Acquire view → compute route → generation still valid?</span></span>
<span class="line"><span>                                      |</span></span>
<span class="line"><span>                         yes ─────────┴──────── no</span></span>
<span class="line"><span>                          |                       |</span></span>
<span class="line"><span>                    accept result          discard result</span></span>
<span class="line"><span>                                           acquire new view</span></span>
<span class="line"><span>                                           recompute</span></span></code></pre></div><p>The caller does not update the pointer inside a stale view. It discards the view and anything derived from it, then calls <code>getMissionView()</code> again and recomputes.</p><p>After an item in a ready generation is patched, a fresh view is available immediately. While a replacement mission is loading, acquiring a new view fails until the complete generation is ready.</p><p>The refreshed view may contain the same pointer address because the backing allocation is reused. The generation, not pointer equality, determines whether its contents are current.</p><p>Use a borrowed view only during serialized Navigator work, and check it with <code>missionViewStillValid()</code> before accepting the result. The generation check is not a lock; another task would need explicit synchronization or its own snapshot.</p></details><details class="details custom-block"><summary>Details</summary><p>Developer details: Keeping mission writes coherent</p><p>Already-loaded mission items are not automatically read from storage again. After a successful active-mission Dataman write, the writer must call <code>syncMissionItem()</code> from Navigator&#39;s task. This call synchronizes the caches; it does not perform the Dataman write.</p><p>For a ready full cache, synchronization patches that item in the array and advances the generation:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Successful Dataman write</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Patch the cached item</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Advance the generation</span></span>
<span class="line"><span>        ↓</span></span>
<span class="line"><span>Old views and route results are stale</span></span></code></pre></div><p>The complete route stays ready. Reloading the whole mission for a one-item change would make it unavailable and add one read per mission item without improving consistency.</p><p>Writes during loading depend on how far the load has progressed:</p><ul><li>An item in the loaded prefix is patched in place.</li><li>A response for the item already being read is ignored, and that item is then read again.</li><li>An unread item is picked up later by the normal load.</li></ul><p>Mission execution follows this process after successful <code>DO_JUMP</code> counter increments and resets. The next route computation therefore sees the current number of remaining loops.</p><p>The mission-land cache is synchronized independently when the written index is the published land item.</p><p>A mission upload or replacement is different from an in-place runtime write. Its new mission identity causes a complete generation to load.</p></details><details class="details custom-block"><summary>Details</summary><p>Developer details: Measuring load time</p><p>Successful, non-empty loads are reported by the <code>navigator: full mission cache load</code> performance counter. It measures wall time until the complete route is ready, including retry delays.</p><p>To measure it on hardware:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">perf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reset</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Upload or change the mission.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">perf</span></span></code></pre></div><p>Replaced, invalidated, empty, or incomplete loads do not add a sample.</p></details>`,17)])])}const m=a(t,[["render",o]]);export{u as __pageData,m as default};
