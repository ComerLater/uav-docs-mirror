import{_ as i,C as s,o as n,c as l,j as t,a as r,E as a,al as h}from"./chunks/framework.7vp1BMOE.js";const v=JSON.parse('{"title":"Motor Failure Recovery","description":"","frontmatter":{},"headers":[],"relativePath":"zh/config/motor_failure_recovery.md","filePath":"zh/config/motor_failure_recovery.md"}'),d={name:"zh/config/motor_failure_recovery.md"};function c(f,e,p,m,u,_){const o=s("Badge");return n(),l("div",null,[e[0]||(e[0]=t("h1",{id:"motor-failure-recovery",tabindex:"-1"},[r("Motor Failure Recovery "),t("a",{class:"header-anchor",href:"#motor-failure-recovery","aria-label":'Permalink to "Motor Failure Recovery"'},"​")],-1)),a(o,{type:"tip",text:"main (PX4 v2.0)"}),e[1]||(e[1]=r()),a(o,{type:"tip",text:"Multicopter"}),e[2]||(e[2]=h('<p>PX4 can reconfigure <a href="./../concept/control_allocation">control allocation (mixing)</a> in flight when a motor failure is detected, so that the vehicle can keep flying on the motors that are left.</p><p>The failure action is selected with <a href="#CA_FAILURE_MODE">CA_FAILURE_MODE</a>. This can be set to <code>0</code> (the default) to simply warn the user, or <code>1</code> to remove the failed motor from allocation. Hexarotor frames provide additional recovery options, which are outlined in the following sections.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>A vehicle flying with a failed motor has less control authority and thrust margin than a healthy one. Fly gently and land as soon as it is safe to do so.</p></div><h2 id="六旋翼飞行器" tabindex="-1">六旋翼飞行器 <a class="header-anchor" href="#六旋翼飞行器" aria-label="Permalink to &quot;六旋翼飞行器&quot;">​</a></h2><h3 id="stop-or-reverse-the-opposite-motor" tabindex="-1">Stop or Reverse the Opposite Motor <a class="header-anchor" href="#stop-or-reverse-the-opposite-motor" aria-label="Permalink to &quot;Stop or Reverse the Opposite Motor&quot;">​</a></h3><p>Modes <code>1</code> and <code>2</code> only differ on a hexarotor; on any other airframe both simply remove the failed motor.</p><p>On a hexarotor the rotor opposite the failed one is the rotor that used to cancel its drag (yaw) torque, so what happens to it decides how much yaw authority is left:</p><ul><li>Mode <code>1</code> stops it, leaving four symmetric rotors and no yaw bias to trim, but a third of the thrust is gone and the heading can still drift.</li><li>Mode <code>2</code> keeps it in the allocation and lets it spin backwards. Driving a rotor in reverse inverts both its thrust and its drag torque, so it can still generate yaw torque. This needs an ESC that can actually reverse the motor.</li></ul><p>The opposite motor is taken from the configured <a href="./actuators#motor-geometry-multicopter">geometry</a>: it is the counter-rotating rotor closest to the failed rotor&#39;s antipode, and it is only computed for a 6-rotor multirotor geometry.</p><h3 id="reverse-thrust-fraction" tabindex="-1">Reverse Thrust Fraction <a class="header-anchor" href="#reverse-thrust-fraction" aria-label="Permalink to &quot;Reverse Thrust Fraction&quot;">​</a></h3><p>A propeller spun backwards produces less thrust than it does forwards, and <a href="#CA_REV_THR_FRAC">CA_REV_THR_FRAC</a> tells the allocator what fraction to expect (default <code>0.4</code>, i.e. 40%, which is representative of a standard multicopter propeller). A symmetric (3D) propeller produces almost the same thrust either way, so it should be set closer to <code>1.0</code>.</p><p>A value in the right region is best, but it is not critical: a hexarotor still recovers with <code>1.0</code> set on a propeller whose real fraction is 0.4.</p><h3 id="esc-requirements-for-reversing" tabindex="-1">ESC Requirements for Reversing <a class="header-anchor" href="#esc-requirements-for-reversing" aria-label="Permalink to &quot;ESC Requirements for Reversing&quot;">​</a></h3><p>Mode <code>2</code> reverses whichever motor sits opposite the one that failed, and any of the six can fail, so every motor on the vehicle has to be able to reverse (not just one of them).</p><p>Reversing needs all of the following:</p><ol><li>Motors and ESCs that can be driven backwards.</li><li>Reversal enabled in the ESC configuration. This is set in the ESC (PX4 does not set it for you).</li><li><a href="#CA_FAILURE_MODE">CA_FAILURE_MODE</a> = <code>2</code>.</li><li>For DShot ESCs only: <a href="./../advanced_config/parameter_reference#DSHOT_3D_ENABLE">DSHOT_3D_ENABLE</a> = <code>1</code>.</li></ol><p>Nothing else is required, in particular the motors do <em>not</em> have to be marked as <a href="./actuators#bidirectional-motors">bidirectional</a> (<a href="./../advanced_config/parameter_reference#CA_R_REV">CA_R_REV</a>). PX4 makes the recovery motor reversible by itself while handling the failure, and returns it to forward-only if the failure clears.</p><h4 id="dronecan" tabindex="-1">DroneCAN <a class="header-anchor" href="#dronecan" aria-label="Permalink to &quot;DroneCAN&quot;">​</a></h4><p>Reverse is part of the protocol: PX4 sends the recovery motor a negative <code>RawCommand</code>, and an ESC configured for bidirectional operation spins it backwards. No additional PX4 parameter is needed. See <a href="./../dronecan/escs#reversible-motors">Reversible motors</a> in <em>DroneCAN ESCs</em>.</p><h4 id="dshot" tabindex="-1">DShot <a class="header-anchor" href="#dshot" aria-label="Permalink to &quot;DShot&quot;">​</a></h4><p>A reversible output is encoded using the DShot 3D split range, where neutral sits in the middle of the range The ESC has to be running in 3D mode, which is a persistent ESC setting (see <a href="./../peripherals/dshot#commands">ESC Commands</a>), and PX4 has to be told about it with <a href="./../advanced_config/parameter_reference#DSHOT_3D_ENABLE">DSHOT_3D_ENABLE</a>. <code>DSHOT_3D_ENABLE</code> puts every motor on the matching encoding. This has nothing to do with <a href="./../peripherals/dshot#bidirectional-dshot-telemetry">Bidirectional DShot</a>, which is about eRPM telemetry rather than reversing a motor.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p><code>DSHOT_3D_ENABLE</code> and the 3D setting in the ESCs must always agree, whether or not a motor has failed. If the ESCs run in 3D mode while <code>DSHOT_3D_ENABLE</code> is <code>0</code>, the lower part of the throttle range is sent in the range those ESCs read as reverse, and the vehicle is not flyable.</p></div><h4 id="pwm-oneshot-and-other-protocols" tabindex="-1">PWM, OneShot, and Other Protocols <a class="header-anchor" href="#pwm-oneshot-and-other-protocols" aria-label="Permalink to &quot;PWM, OneShot, and Other Protocols&quot;">​</a></h4><p>These cannot reverse a motor, so <a href="#CA_FAILURE_MODE">CA_FAILURE_MODE</a> has to be <code>1</code> or <code>0</code>. With mode <code>2</code> the recovery motor would sit at around half throttle forwards instead of reversing, which is worse than stopping it.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>PX4 does not check whether an ESC can reverse, and a reverse command sent to an ESC that is not set up for it comes out as forward thrust. Before flying with mode <code>2</code>, confirm on the bench that each motor really does spin backwards, for example from the sign of the reported RPM.</p></div><h2 id="failure-detection" tabindex="-1">Failure Detection <a class="header-anchor" href="#failure-detection" aria-label="Permalink to &quot;Failure Detection&quot;">​</a></h2><p>Recovery reacts to the motor failure flag raised by the <a href="./safety#motor-failure-trigger">failure detector</a>, which is set when either:</p><ul><li>an ESC stops sending telemetry, or reports a fault. This requires <a href="./../advanced_config/parameter_reference#COM_ARM_CHK_ESCS">COM_ARM_CHK_ESCS</a> to be enabled.</li><li>the current reported by an ESC is outside the band expected for its commanded thrust. This requires <a href="./safety#FD_ACT_EN">FD_ACT_EN</a> to be enabled, and the <a href="./safety#motor-failure-trigger">MOTFAIL_*</a> thresholds to be tuned for the vehicle.</li></ul><p>Either path needs ESC telemetry, so recovery is only possible with telemetry-capable ESCs (such as <a href="./../dronecan/escs">DroneCAN</a> or <a href="./../peripherals/dshot#esc-telemetry">DShot with telemetry</a>). Note that the two are gated by different parameters: an ESC that goes silent triggers recovery with <code>FD_ACT_EN</code> disabled, as long as <code>COM_ARM_CHK_ESCS</code> is enabled.</p><p>The current-based check <strong>latches</strong>: once a motor is flagged it stays flagged until the vehicle disarms. This is deliberate, because the recovery stops the failed motor, which would otherwise make it look healthy again and clear the failure. The ESC offline/fault check does clear if the ESC starts reporting again, in which case all motors are restored to the allocation and any runtime reverse is removed.</p><p>To exercise the whole chain you can take an ESC offline with <a href="./../debug/failure_injection">failure injection</a>: <code>failure esc off -i &lt;n&gt;</code>.</p><h2 id="参数" tabindex="-1">参数 <a class="header-anchor" href="#参数" aria-label="Permalink to &quot;参数&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Parameter</th><th>描述</th></tr></thead><tbody><tr><td><a id="CA_FAILURE_MODE"></a><a href="./../advanced_config/parameter_reference#CA_FAILURE_MODE">CA_FAILURE_MODE</a></td><td>What to do on a single motor failure. <br><code>0</code> (default): Ignore and report failure.<br><code>1</code>: Remove failed motor from allocation. Hexarotor: also stop the opposite motor.<br><code>2</code>: Remove failed motor from allocation. Hexarotor: also reverse the opposite motor.</td></tr><tr><td><a id="CA_REV_THR_FRAC"></a><a href="./../advanced_config/parameter_reference#CA_REV_THR_FRAC">CA_REV_THR_FRAC</a></td><td>Fraction of forward thrust that the recovery motor is expected to produce in reverse (default <code>0.4</code>). Only used by mode <code>2</code>.</td></tr></tbody></table><h2 id="另见" tabindex="-1">另见 <a class="header-anchor" href="#另见" aria-label="Permalink to &quot;另见&quot;">​</a></h2><ul><li><a href="./../config/safety">Safety Configuration (Failsafes)</a>, covering the failure detector and motor failure detection.</li><li><a href="./../concept/control_allocation">Control Allocation (Mixing)</a></li><li><a href="./actuators">Actuator Configuration and Testing</a>, covering geometry, motor order and bidirectional motors.</li></ul>',35))])}const b=i(d,[["render",c]]);export{v as __pageData,b as default};
